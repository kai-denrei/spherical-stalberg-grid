# Handoff — overnight 2026-09-05

For a fresh Claude session on kainode, in this repo. Read `CLAUDE.md`
first (the hard rules: `./scripts/bust.sh --quiet` after any src/html/css
edit, commit ATOMICALLY with everything bust touched, `npm test` green,
one Chrome at a time, headless recipes), then `.deban/_index.md`, then
this.

The operator is asleep. Their standing instruction for a night like this
(2026-09-04): dispatch an agent to take design decisions on their behalf
where the repo's intent is clear; Telegram on milestones, not chatter;
never claim done without the artifact verified. Cinematics FRAMING and
phase 5 are on hold by their word — do not touch the five director clips'
rails or the lock-screen attract mode.

## State at handoff (`0506c0c`, tree clean, Pages live)

- Board: the weathered metal is ON by default (`?metal=0` off): casts get
  the sky bake as environment and the albedo as emissiveMap under the
  kept grey ladder (`board` preset). Verified on stills — containers read
  mottled; the tank dresses once its model lands (10 materials).
- Tab bar grouped (play / studies / cine / research / tech). `#astro` is
  the astronaut study (one walk clip, 25 bones, the tank beside it).
- The phone's third-person camera: FOUR fixes shipped by reading, none
  confirmed on the device. The device has a diagnostics overlay now
  (tap the hearts ×3, or `?diag=1`) and a view watchdog. The operator
  has not yet sent the overlay screenshot. DO NOT guess a fifth fix; wait
  for the screenshot (Telegram) — the .deban dev.md dead ends say why.
- The director (`#td?director=NAME`) works and is deterministic with
  `&seed=4414&sky=4414`; scripts in `src/cine/scripts.js`.

## Work for the night, in order

### 1. THE MINES — build the spec (`docs/superpowers/specs/2026-09-04-mines-design.md`)

The operator approved the mechanics ("we will test") and asked for the
spec, which is written. Not yet explicitly approved for the plan step;
their intent is clear enough to build overnight. Use the
`superpowers:writing-plans` skill first (a plan file under
`docs/superpowers/plans/`), then implement task by task with tests:

1. `src/mines.js` pure + `test/mines.mjs` (register in package.json).
   Use `arc.js` for arc length. Every test in spec §6.
2. td-tab: the field, `N` / `#td-pad-mine` (index.html + CSS beside the
   FIRE/LASER pads), the drop one cell ahead, the per-frame trip test,
   the blast via `damageEnemy(…, 'mine')`, `popPods` + hp−1 on a gate,
   1 HP to the tank, the chain over following frames. HUD count beside
   `✦` ammo. Sounds: `tank_pickup` (drop), `tower_slow` (arm),
   `tank_main` (blast).
3. Visuals: the dot-cloud disc (arming dim / live lit), the red fan on
   the ground (`params.mineArcs`, `?minearcs=0`), the radar arrowhead
   (`radar.js` contact API — friendly-blue, hollow while arming).
4. Supply: `TF` gets `caseEvery: 3` (a case of 5 on the yard clock; the
   yard readout names it); `SINK.mines = 200` and a debrief row; cap 20.
5. `?mines=N`, `?mineprobe=1` (lay three across a lane, walk a phage
   through, log trips / targets / chain / tank HP). A DEVLOG entry.
   Telegram when the probe passes.

Do NOT add the fused / two-kind / chasing mines (A/C/D, shelved).

### 2. The astronaut — the study's next question

`#astro` shows the walk. The rescue mission needs at least an idle and a
"come here / help" gesture. Investigate (do not buy, do not fetch from
the net without noting it): can a Mixamo-style clip be retargeted onto
this rig's bone names (`Hips_00`, `Spine_011`, `LeftArm_013` …)? Write
the finding into `docs/CINEMATICS-PLAN.md` §10 or a new
`docs/RESCUE-MISSION-NOTES.md` — a note, not a build.

### 3. If tokens remain

- Director sound: a rail per script in `src/cine/sound.js`
  (`SOUND_RAILS.ram/strike/tank/gate/planet`), muxed onto the five clips
  with `scripts/cine-mux.mjs` (the clips are in `renders/`, gitignored).
- The metal on the board on the PHONE tier: verify with the shell replica
  (`?mobile=1&coarse=1&tutorial=0&intro=0&autoplay=1&seed=4414#td`, size
  908x418) that 256-px maps cost nothing visible and read at all.

## Things you will want

- Replicas: `node scripts/headless-wait.mjs --url … --seconds N --size WxH
  --out x.png --grep REGEX` (real time). Desktop return-visit path:
  `?cine=0&tutorial=0&intro=0&autoplay=1&seed=4414#td`. Shell:
  add `mobile=1&coarse=1`. The state line: `?diag=1` prints `DIAG` every
  2 s (whole line — the runner prints 2000 chars now).
- A `let` declared far down `initTdTab` is a TDZ trap for any code that
  runs at init (three of those today). Read early state through
  `root.querySelector` / a local `URLSearchParams`.
- Chrome's launch mode retries a cold first frame; CDP mode at ≤1280 px
  wide is fine for the board.
- Telegram: `~/CLAUDE.md` has the curl; chat id 496805857.
- Vault: `~/Documents/kainode-vault/history/2026-09-04-overnight-cinematics.md`
  is the running entry; start `2026-09-05-…` for the night.
- `.deban` sync at the end (`/deban sync`).

## Open questions for the operator (do not decide these)

- The mine's numbers after play (arm 2 s, fan 1 cell, 120°/2.5 cells,
  a case of 5 every 3 waves, 200 kg, cap 20).
- The phone camera: the overlay screenshot.
- Safari → Develop → Allow Remote Automation on kainode (one click).
- The director clips' framing notes; director sound; phase 5.
